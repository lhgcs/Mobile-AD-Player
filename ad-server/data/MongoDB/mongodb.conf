# 数据存储目录
dbpath=/home/ubuntu/Desktop/Mobile-AD-Player/ad-server/data/MongoDB/data
# 日志文件路径
logpath=data/MongoDB/log/mongodb.log     
# 追加
logappend = true 
# 端口号
port=27017 
# 后台进程
fork=true 
# 必须绝对路径，必须一行，不能有注释